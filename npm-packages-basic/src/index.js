import chalk from "chalk";
import logSymbols from "log-symbols";

console.log(chalk.bgRed.yellow("                    "));

console.log(logSymbols.success, chalk.green("servidor Iniciado"));

console.log(logSymbols.error, chalk.green("servidor falha"));

console.log(logSymbols.info, chalk.green("servidor falha"));

console.log(logSymbols.warning, chalk.green("servidor falha"));